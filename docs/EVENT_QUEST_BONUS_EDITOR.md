# QuestBonus 活动共鸣配置

入口：管理页 `/admin/activities` → `EventQuestChapter` → 活动行「共鸣配置」，或 `/admin/related` →「QuestBonus」。

## 页面与选择器

QuestBonus 沿用其他表的页面布局、统计区、工具栏和底部保存栏。日期跟随顶部「时间基准」选择，可使用本机时间或 UTC。

数据表选择器为普通下拉框，按显示名称排序，仅显示名称，不显示行数及 `m_*` 原始表名。其他大量带标题的选项使用搜索下拉框，支持标题／ID 搜索、键盘选择和虚拟列表。任务奖励及任务期限的搜索框在首次渲染时就可使用，完整选项仍按需加载。

## 编辑流程

1. 搜索目标活动标题或章节 ID，同名复刻活动按各自 ID 分别处理。
2. 在「历史名单来源」选择任意已有加成条目。可以搜索加成 ID、服装／武器组 ID、成员名称。完全相同的引用合并展示，输入完整加成 ID 仍可选择具体条目。
3. 左右核对当前名单和还原名单。历史来源提供完整服装成员及效果，以及完整武器成员；同一进化组的武器形态归并显示。
4. 武器规则默认参考本活动。同一武器家族自动匹配，新增武器需选择一个现有完整规则，涵盖各关卡分组、突破档位和进化形态。
5. 「关卡预览」和「突破」只切换预览。常规、EX Hard 银奖章和金奖章关卡保留各自规则，不会统一替换成一个历史加成 ID。
6. 活动没有现成规则时，选择其他「武器规则参考」活动，并在「关卡／奖章对应」中明确选择各关卡的参考分组和目标活动实际奖章。
7. 点击「重建并应用」核对保存预览后确认。预览横向对照各阶段的实际奖章和 0～4 突破数量。可准备多个活动，或撤销单个活动、放弃全部草稿。

共鸣没有独立期限，全部与目标活动起止时间相同，后续修改活动时间时自动同步。按需复制成员组及加成记录，复用现有效果，避免修改其他活动共用的配置。实际共用同一关卡的活动无法使用各自独立期限，保存时会要求先分离关卡引用。

## 保存与校验

- `GET /api/admin/master-data/table?name=m_quest_bonus`：按需返回活动、关卡、加成定义、本地化名称、武器进化关系及实际奖章。
- `POST /api/admin/master-data/schedules/preview`：接收 `questBonusRestores`，生成最终关卡分组、期限及成员效果供核对。
- `POST /api/admin/master-data/schedules`：按同一请求重建并应用。

请求包含 `expectedVersion`、目标活动 ID、历史来源 ID、参考活动 ID、武器规则，以及跨活动引用时的关卡／奖章对应关系。预览和保存执行同一个生成流程，继续使用版本冲突检查、引用校验、候选主数据验证、备份、原子替换及热重载。

代码更新本身不改变主数据。重建并重启服务后载入新版内嵌管理页，随后可以通过管理页应用配置。
